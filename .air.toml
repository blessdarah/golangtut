root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/api"
  bin = "./tmp/main"
  include_ext = ["go", "toml"]
  exclude_dir = ["tmp", "internal/db/migrations"]
  delay = 1000
  stop_on_error = true
  poll = false

[log]
  time = true

[misc]
  clean_on_exit = true